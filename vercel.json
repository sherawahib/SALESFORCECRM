{
  "installCommand": "npm install && cd v2 && npm install",
  "buildCommand": "cd v2 && npm run vercel-build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 12 * * *"
    }
  ]
}
